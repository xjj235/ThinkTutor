NODE_ENV=production
# Dedicated short-lived migration role. It may alter only the ThinkTutor schema
# and must not be supplied to the web or worker containers.
DATABASE_URL=postgresql://thinktutor_migrator:REPLACE@private-rds-host:5432/thinktutor?sslmode=require
