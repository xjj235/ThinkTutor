# ThinkTutor 设计系统

本文件为全站视觉与交互依据。2026-09-04 选择浅色专业工作台；2026-09-07 改为顶部导航；2026-09-09 至 10 日按唯一 shadcn/ui new-york-v4 基准重建样式层。
设计改版不改变产品角色、安全边界或知识包发布条件。

## 视觉方向

近白内容画布、中性导航、精密排版与明确的信息层级。默认浅色，可切换深色。
炭黑用于主操作，青绿仅用于品牌、有效状态与少量文本动作；信息状态使用中性色，红色用于异常。
不使用虚构趋势图、数据、成绩或教师确认记录。

## 核心原则

1. 页面是工作空间，不是堆叠的卡片。使用分隔线、共享基线与留白组织内容。
2. 学习记录和独立表达是主体；分数辅助定位，证据支持评价。
3. 功能名称准确一致，避免口语化宣传、夸大承诺与无意义术语。
4. 保留所有角色权限、交互状态、错误反馈和可追溯信息。

## 色彩令牌

| Token | Value | 用途 |
|---|---|---|
| --background / --card | #fafafa / #fdfdfd | 画布 / 内容面；暗色 #0a0a0a / #171717 |
| --foreground | #171717 | 正文；暗色 #fafafa |
| --muted / --muted-foreground | #f5f5f5 / #666666 | 次级信息面与文字；暗色 #262626 / #b0b0b0 |
| --border / --input | #e5e5e5 / #888888 | 分隔与控件；暗色 #383838 / #808080 |
| --primary / --primary-foreground | #262626 / #fafafa | 主按钮；暗色 #e5e5e5 / #171717 |
| --brand / --brand-subtle | #16776a / #eaf5f0 | 品牌与有效状态；暗色 #79d4bf / #173a32 |
| --destructive | #b42318 | 危险动作；暗色 #ffb4aa |

旧 paper/ink/teal 等名称仅为兼容别名，不再构成另一套主题。

## 排版与尺寸

- 系统 CJK 无衬线字体，不依赖远程字体。
- 五档字号：14/16/18/20/32px；移动端不另设一套字级。
- 界面文字与表单标签16px；状态、计数和元数据14px。
- 长篇研习记录18px/32px，最长44em；独立作答文字18px/32px。
- 普通输入文字在桌面与移动端均为 16px，学习表达输入为 18px。
- 公共入口标题32px；不按视口宽度缩放字号。
- 字距为 0；数值使用等宽数字，不对正文使用等宽字体。
- 标签与控件圆角6px、记录与重复项目卡片8px；圆形仅用于头像、阶段序号与单选框。
- 主要操作与表单控件高 48px；独立图标按钮至少 44px。
- 共享间距4/8/12/16/24/32/40/48/64px；默认行内16px、分组24px、区段32px。
- 页面边距桌面32px、平板24px、手机16px；列表标题与数据使用相同栅格。

## 页面结构

- 不设置全站左侧导航。桌面品牌栏72px、横向导航48px；移动端品牌栏64px。
- 960px 及以下从顶部展开导航，菜单占据正常文档流，保留全部角色入口。
- 页面顶层统一1280px居中；学习阅读区1040px、文本最长44em，作为内层阅读约束。
- 学习总览：真实统计、研习记录、学习安排。无记录时显示明确空状态。
- 学习页：顶部任务上下文、横向阶段进度、下方研习记录与独立作答区。
- 创建任务：课程关联、研习目标、补充资料分组；桌面短字段并排、长文本通栏，移动端单列。提交操作单独成行。
- 教学表单：课程与任务的短字段双列、说明文本通栏；移动端单列，不改变字段含义与提交逻辑。
- 报告：评估摘要、五维能力评估、学习证据与定向巩固。
- 知识审核：版本概览、来源确认、样例校准、审核记录。
- 公共入口复用真实学习场景位图，不使用左右卡片式英雄区。

## 交互与无障碍

- 使用 Lucide 图标；图标按钮必须具有可访问名称和提示。
- 保留加载、成功、失败、重试、禁用和空状态。
- 反馈文字16px，状态图标20px；禁用采用独立背景/文字，不整体降低透明度。
- 交互色彩过渡180ms，状态不移动点击目标；低动态偏好关闭过渡及加载旋转。
- 顶部菜单使用自然键盘顺序，不遮挡内容；Escape 关闭并恢复焦点，切换到宽屏时重置展开状态。
- 横向阶段栏可聚焦滚动；阶段推进或尺寸变化时只横向定位当前阶段，不移动页面。
- 标签页支持方向键及 Home/End，未选中的内容不暴露到键盘焦点顺序。
- 当前页面和学习阶段使用 aria-current，状态不只靠颜色表达。
- 表格在窄屏内滚动；不裁切内容来掩盖溢出。
- 尊重减少动态效果的系统设置；不使用装饰性连续动画。

## 用语规范

- 学习总览 / 教学总览 / 管理总览：角色首页。
- 自主研习 / 课程任务 / 学习档案：学习入口与记录。
- 认知诊断 / 知识建构 / 迁移验证 / 费曼阐释 / 反思修订：教学阶段。
- 五维能力评估 / 待巩固要点 / 定向巩固：形成性反馈。
- 按钮直接表达动作，保留“登录”“提交回答”等明确命令。
- 原始学习证据、模型回答和评分结论不得为视觉包装而改写。

## 实现与来源

主题数值唯一来源为 src/app/tokens.css；基础控件和Markdown规则位于globals.css，页面构图位于workspace.css；两份样式已整体重建。
研究来源、借鉴范围和本轮验收记录见 docs/UI_REDESIGN_RESULT.md。
顶部导航改版记录见 docs/UI_TOP_NAVIGATION_REDESIGN_2026-09-07.md。
本轮 GitHub 参考、字号对照和页面验证见 docs/UI_READABILITY_REDESIGN_2026-09-09.md。
本轮唯一基准见 docs/GitHub-SHADCN-数值参照.md；新基线审计见 docs/UI-SHADCN-改造前审计.md；当前验收见 docs/UI-改造总结.md。此前报告保留为历史，不是当前验收证据。
